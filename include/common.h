// Common definitions etc. for C/C++ code in the text editor

#pragma once

// Definitions
#define ORIGIN_REPO_URL "https://github.com/v-superuser/text-editor"

// Math
#define MAX(x, y) ((x) > (y) ? (x) : (y))
#define MIN(x, y) ((x) < (y) ? (x) : (y))
